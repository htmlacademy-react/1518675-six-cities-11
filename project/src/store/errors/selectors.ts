import {UserError} from '../../types/data-type';
import {NameSpace} from '../../const';
import {State} from '../../types/state';

// export const setError = (state: State): string => state[NameSpace.Data].error;
